export function capitalizeFirst(value) {
  return value.charAt(0).toUpperCase() + value.slice(1);
}

export function nothing() {
  // just placeholder to prevent eslint from complaining default export
}
